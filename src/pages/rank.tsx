import { type NextPage } from "next";
import Leaderboard from "../components/Leaderboard";

const Rank: NextPage = () => {
  return (
    <div>
      <Leaderboard />
    </div>
  );
};

export default Rank;
